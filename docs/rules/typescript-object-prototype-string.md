# `typescript-object-prototype-string`

**Purpose:** Prototype inspection is a weak type contract.

```ts
Object.prototype.toString.call(value)
```

**Fix:** Use a named type guard or an explicit value check.
