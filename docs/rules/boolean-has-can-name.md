# `boolean-has-can-name`


Review boolean methods beginning with `has*` or `can*`. These are not absolute
bans. When a natural property exists, prefer `accessible()`, `present()`,
`writable()`, or `readable()`. Do not invent an awkward adjective merely to
satisfy the rule.
