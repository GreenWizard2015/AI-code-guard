# `directory-class-max-size`

**Purpose:** Detect source directories with more classes than the maximum class boundary.

**Fix:** Hide implementation details one level below or move one cohesive group to a focused parent
or child directory. Do not add placeholder classes or files.
