# cyrillic-comment

Comments and Markdown documentation use Latin script. Cyrillic text is allowed inside source string literals.

```ts
// Localized text belongs in a string literal.
const message = "localized text";
```

Keep user-facing Cyrillic in data or string literals. Write comments and Markdown in Latin script so project documentation has one consistent language.
