# `boolean-equals-name`


Review boolean `equals()` methods. When the language protocol permits it, a
property-shaped name such as `equalTo(other)` better expresses “is equal to”.
Do not rename a required language equality protocol mechanically.
