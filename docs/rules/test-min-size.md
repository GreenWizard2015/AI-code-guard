# `test-min-size`

Tests must contain enough real setup and result verification to represent a
meaningful behavior.

**Fix:** add the missing behavior assertion or combine the test with a meaningful
scenario. Do not add padding only to satisfy the size limit.
