# `module-types-separation`

Module-level types must be separated from implementation code in every ordinary file.
Move them to the exact sibling `types.ts` or `types.py`, or to one focused file when the types
belong together.

Do not keep module-level types beside implementation code.
