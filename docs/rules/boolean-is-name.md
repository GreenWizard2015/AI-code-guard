# `boolean-is-name`


Review boolean methods beginning with `is*`. Prefer an adjective when it reads
naturally: `isEmpty()` → `empty()`, `isReadable()` → `readable()`, and
`isNegative()` → `negative()`.
