# `type-field-count`

**Purpose:** Keep TypeScript type contracts, interfaces, and Python type contracts focused.
Every type may declare at most 10 fields.

**Fix:** Split a large contract into focused named types or compose smaller types.
