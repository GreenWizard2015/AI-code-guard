# `python-test-shape`

**Purpose:** Production object boundaries should be exercised consistently by Python tests.

```py
def test_behavior(): ...
```

**Fix:** Use class methods when the repository's Python test contract requires class-based tests.

